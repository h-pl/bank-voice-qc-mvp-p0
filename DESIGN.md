---
version: alpha
colors:
  primary: '#20252b'
  canvas: '#f3f5f7'
  surface: '#ffffff'
  ink: '#20252b'
  muted: '#626b76'
  line: '#e1e5e9'
  accent: '#f47b34'
  accentInk: '#a64719'
  accentSoft: '#fff3e9'
  success: '#187349'
  danger: '#b52b38'
typography:
  body:
    fontFamily: '-apple-system, BlinkMacSystemFont, PingFang SC, Microsoft YaHei, sans-serif'
    fontSize: '14px'
    lineHeight: '1.6'
  heading:
    fontFamily: 'Avenir Next, PingFang SC, Microsoft YaHei, sans-serif'
  data:
    fontFamily: 'SFMono-Regular, Consolas, Liberation Mono, monospace'
rounded:
  control: '7px'
  panel: '12px'
spacing:
  controlGap: '12px'
  panelGap: '20px'
  section: '24px'
---

# Moss Quality · 银行质检作业台

## Overview

为质检主管、质检员、客服坐席设计的中文桌面工作台。依据 docs/reviews/PRD-银行语音质检-MVP-P0.md，维持三个角色、七个业务模块和灰白橙黑身份。页面单一标题在顶栏，所有常用筛选与可执行动作常显。

使用原有灰白橙黑配色：黑色主动作、橙色导航与选中定位。参考 Moss API 企业控制台的筛选与就近反馈、3008 的案件分层，而不照搬整页或切换为黑白主题。优先让操作者定位案件、核对摘要、预览材料并执行下一步；辅助说明通过信息入口按需阅读，不占用主要操作位。

## Colors

运行时 token 为唯一值所有者（Model B）：app/workspace-system.css 的 :root → 全局共享组件 → 各业务页面。本文件镜像已接受值；app/globals.css 保留业务布局历史基线，共享系统由 workspace-system.css 最后统一。

黑色用于主动作；橙色点缀不承担正文对比度，正文强调使用 accentInk。成功/危险状态必须同时显示文字。浅灰画布与白色内容区保持对比，风险证据采用 accentSoft。只支持浅色主题；不凭空增加深色模式。

## Typography

正文及证据采用系统中文字体，14px 正文，14px 控件，12px 次要说明；标题用 Avenir Next / PingFang SC，19px 顶栏、24px 事项/规则/资源标题、16px 分区标题（较窄桌面事项标题 21px）。编号与时间使用等宽字，数字使用等宽数字。无需远程字体加载，不发生字体切换抖动。

## Layout

以桌面操作效率为中心。左导航与 68px 顶栏保留；风险、工单、申诉整改与通话详情共用 CaseShell、CaseResponsibility 和下一步操作结构。常用筛选、分类和分页保持在工作区边界，队列、案件内容和责任栏各自滚动，禁止长证据撑高整页。案件与责任栏顶部对齐，主动作在辅助说明和历史之前。次动作两列紧凑排列，不隐藏在“更多”菜单。

案件首屏包括摘要、状态、责任、证据与材料入口；完整录音、转写、规则快照和长材料进入 960px 上限的预览 Modal。预览关闭后恢复原位置和触发者焦点。案件原结论、申诉理由、整改标准、复核意见与阶段差异真实保留。

规则保留目录与详情，参数在 560px 编辑弹窗内修改；底页保持当前值与位置，保存后显示待发布草稿；未保存退出提供继续编辑和放弃修改。指标目录作为 info 辅助入口放在规则指标信息旁。资源按内容、引用、版本阅读。规则与资源目录、正文在桌面独立滚动，标题保持可见。

通话筛选采用关键词、业务与筛选展开入口；收起后所有已生效条件持续显示，可单独移除或全部重置。报表保留分析布局，趋势点附近显示日期、指标值和样本口径，环图与条形图显示类别、数量与占比；点选进入实际明细。

## Elevation & Depth

白色面板用细边框分层，不使用常态投影；强阴影仅用于弹窗。禁止为每个字段单独加卡片。选中态使用背景、定位线和 aria 状态共同表达。

## Shapes

控制圆角 7px，面板 12px，状态标签 5px。图标沿用 components/icon.tsx 的线性 SVG。按钮按下改变背景，不缩放、不移动。

## Components

| 文档语义 | 运行时 token | 消费者 |
| --- | --- | --- |
| canvas / surface / ink / muted / line | --canvas / --surface / --ink / --muted / --line | 页面骨架、文字、面板 |
| accent / accentInk / accentSoft | --orange / --accent-ink / --accent-soft | 证据、业务链接、图表 |
| selection | --selection-bg / --selection-border | 导航、队列、版本选中、共享页签 |
| geometry | --control-height / --section-padding / --section-title-size | 控件高度、分区内边距、分区标题 |
| success / danger | --green / --red | Badge、危险按钮、字段错误 |
| body / heading / data | --font-body / --font-heading / --font-data | 正文、标题、编号与时间 |
| control / panel | --radius-control / --radius-panel | Button、SearchField、面板、Modal |
| scrollbar | --scrollbar-thumb / --scrollbar-track / --scrollbar-hover / --scrollbar-active | 所有应用滚动区域 |

共同组件与交互见 UX-CONTRACT.md。SearchField 保留清除按钮空间；Tabs 采用 shadcn/Radix 的键盘与焦点管理；Modal 使用 shadcn Dialog 的焦点锁定和背景隔离。Toast 淡入；图表浮层按下述动效规范过渡，reduced-motion 禁止动画；强制色模式保留系统滚动条与选中轮廓。

## Do's and Don'ts

- 保留三角色权限、版本快照和真实样例状态，不为视觉演示制造经营数字。
- 不将未播放录音的首句话标为正在播放；无法播放必须给出恢复入口。
- 规则结构固定时，用一条说明代替空的参数配置区，保留资源调整路径。
- 正文不使用低对比度浅灰；键盘焦点必须可见；高频按钮不放进“更多”。

## Reconcile drift

| 基线偏差 | 本轮处理 |
| --- | --- |
| 规则与业务搜索各自实现且无清除 | 统一 SearchField，保留 IME 组合输入 |
| 作业/详情/通知页签不一致 | 统一 Tabs，与原报表键盘模式对齐 |
| 待办与移动导航没有模态焦点隔离 | 复用 Modal 的 notification / navigation 变体 |
| scrollbar 未定义、旧 textarea 可拖动 | 全局 token 基线、自动增高且有界的 textarea |
| 页面标题一直相同 | 导航同步 document.title |

## Cross-page interaction system · 2026-09-13

统一的是任务的定位、核对、操作和反馈方式。作业页共用桌面工作区，规则保留目录阅读，资源采用分类表格与独立详情，报表保留分析模式；不以响应式或简单颜色替换作为本轮重点。

UI 公共组件承载焦点、禁用、忙碌和状态表达，业务分区承担不同任务。案件固定工作区消费浏览器可用高度，内部滚动容器都可通过键盘聚焦后滚动；短内容不强行垂直均分。长内容由预览和明确的完整材料入口承载，摘要截断不丢弃原文。

## 6001 accepted iteration · 2026-09-20

来源：用户要求将新版/旧版两轮对比落盘，并按建议在新版代码上开分支运行于 6001。范围与采纳表见 docs/reviews/新版与旧版-需求逻辑架构及方案汇总-20260920.md 和 新版与旧版-UI交互对比与采纳方案-20260920.md。

- 工作台按当前身份汇总真实待办、逾期与 24 小时内到期任务，点击进入原业务对象；不新增模拟经营数字。
- 业务资源以分类表格为入口，SearchField、Tabs、Pagination 继续复用。点击资源名称以 resource Modal 变体打开居中预览；完整详情独立呈现，新增及编辑使用 ActionForm 操作弹窗。表格筛选、分页与位置保留；详情版本与引用明确分开。
- 案件材料按原始结论、申诉理由、整改要求、完成材料、验收意见列出真实摘要与状态，各条可独立预览。复核页优先展示逐项意见。材料内容不改变底层快照。
- 当前责任增加业务“后续去向”；三个作业页仍共用 CaseResponsibility 和 actions/primaryAction，不以按钮位置推导新权限。
- 资源发布与规则引用切换拆开；界面使用“最新已发布”和“当前引用 Vx”，不将已发布资源误称为已被所有规则使用。
- 本轮验证重点为 1440×900、1280×720 桌面任务区域，保留原色，不将断点调整作为交付重点。

## 图表浮层 callout · 2026-09-20

用户以 Moss API usage3 截图纠正上一轮解释：callout 是带日期、指标与数值的白色浮层，不是每点数值标签或环图引线。趋势默认选择最后一个有样本日期；整个绘图区按鼠标横向位置吸附最近日期，无须精准命中点，移开后保留。环形图默认选择第一个非零分类，扇区/图例悬停与键盘焦点切换浮层内容，移开后保留。一次一个浮层，沿用原橙色系、细边框与浅色面积；点击明确下钻，零分母显示 —。


图表动效以实际 usage3 页面为依据（DOM 显示 tooltip 为 transform 400ms）：浮层通过 transform 平滑位移 400ms，参考线和选中点 180ms，环图扇区强调 220ms、浮层内容 160ms；趋势首次展示/指标切换描线 650ms、面积淡入 450ms。鼠标更新经 requestAnimationFrame 每帧合并，键盘选择取消待处理的鼠标帧；不在悬停时重启动画或改变数据。prefers-reduced-motion 关闭上述动画和过渡。


## B2B UI refinement · 2026-09-20

依据 b2b-ui-design 的视觉关系与工作台适配：保持暖橙业务强调、黑色主动作和现有系统中文字体（未迁移 Sora/MiSans，不宣称字形一致）。将事项正文恢复到 14px，辅助文案 12px；侧栏近白，主区以细线和区域间距分组。1920 桌面采用 290px 队列、310px 责任栏，中间留给事实和依据。复核、风险、申诉整改共用队列标题、专注办理开关及前后事项导航；折叠不卸载队列和详情，保留当前页签和滚动位置，切换事项沿当前排序跨分页浏览。录音入口带文字；复核判定与意见分层表达。规则、资源、工作台复用相同正文和标题尺度。控件 hover 140ms，弹窗进入 180ms，资源预览同样居中显示，减少动态效果时关闭。运行时仍由 workspace-system.css 统一所有权。

常用桌面高度下，责任卡只包含处理人、阶段与期限；处理后去向及辅助说明排在主要操作之后，防止主按钮被低频信息挤出首屏。

### 操作弹窗精简（2026-09-20）

依据本轮实查的 Moss API Key 创建弹窗，操作表单采用单层白色工作面：680px 宽度上限、16px 圆角、24/28px 主间距，标题 20px、字段 14px；黑色 72% 遮罩。动作与对象由标题和一行副标题表达，取消重复编号卡片。复核问题以编号标题和细分隔线分组，桌面结论选择与标题同行，禁止 fieldset 层层套框。该外观现扩展到全部业务弹窗；阅读内容也采用同样标题、遮罩和动作区。

正文单一滚动，标题与底部动作固定；校验反馈位于正文之后、按钮之前，必填字段仍聚焦并标记错误。证据默认展示按时间排序的已选原文，调整时展开同一正文内完整列表，收起保留选择；无选中项初始展开。操作弹窗不因遮罩点击关闭，Escape/关闭/取消沿用现有退出行为并恢复触发者焦点。资源新增及编辑使用同款操作弹窗，保留底页与离开保护。


### 全部弹窗统一

Modal 为所有弹窗的共同入口，提供标题、描述、关闭、正文滚动区和 footer 插槽。业务弹窗共用 72% 黑色遮罩、16px 圆角、20px 标题、14px 正文、24/28px 间距和 180ms 入场；均不通过遮罩点击关闭，Escape 走各自的退出逻辑。窗口按内容自适应高度，上限 880px / 视口减 48px；仅正文滚动，按钮稳定可见。

规则参数 560px，普通动作、案件材料、文档及资源预览 680px，录音长证据 960px，待办 520px。差异只在容量和内容，资源预览不再使用独立右侧抽屉。移动导航保留导航侧栏位置与宽度，但共享标题、关闭和遮罩机制。案件材料去掉套框，用标题和细分隔组织。资源返回栏采用左箭头文字入口，返回保持列表分类与条目焦点。

资源编辑/新增弹窗宽度 680px，初始焦点进入内容或名称；长表单只滚动正文。未保存关闭时，提示与继续编辑/放弃修改固定在底部，不叠加第二个弹窗。取消恢复入口焦点与原资源位置；保存只产生草稿，发布仍走已有检查及发布流程。


### 详情返回导航统一

资源详情和通话详情共用 DetailNavigation：白底单行、57px 基准高度、左右 24px 内边距、13px 左箭头文字入口。通话详情移除独立的来源返回条和筛选保留提示，取消正文外重复套框。跨页进入时左侧显示具体来源名称，通话列表作为同行次级入口；列表进入时直接返回通话列表。前后通话按钮使用 32px 高度。业务内容和两页布局仍各自保留。


### 分析与工作台页面结构统一

页面外框沿用 panel 的白底、边界和圆角。我的待办和质量报表直接从任务范围或筛选区开始，页面身份统一由顶栏表达；业务资源可使用表达具体业务内容的分区标题。工作台移除大标语及分散统计卡片，用单一主面板组织任务；报表各子模块的筛选、统计、图表、明细纳入同一主面板，以细分隔区分而非多层卡片。

工作台与分析页共享 MetricSummary：28px 数字、12px 口径说明、橙色选中标记。工作台仅保留指标摘要这一套待办范围筛选，取消相同功能的重复页签；正文仍为待办主区和最近变化辅区，桌面待办列表内部滚动。分析页仍保留四个分类、日期及业务范围、趋势/环图、下钻明细；统计口径和图表 callout 动效不变。页面结构一致性针对容器、标题、控件与层级，不强制业务布局相同。


### 质量报表模块与图表容器修复

左侧原“分析”分组改名“质量报表”，其下平铺质检概览、问题分布、坐席 / 班组、申诉与整改四个子模块，菜单样式及左对齐与其他业务分组一致。没有中间折叠父菜单，也没有页面内页签。各模块独立 URL（view=reports&module=overview/issues/teams/improvement），独立标题、筛选及分页状态；旧 reports 链接默认进入质检概览。

趋势图不使用滚动容器或 520px 最小画布宽度。ResizeObserver 根据实际绘图区计算 SVG 坐标，保持轴文字可读；图高 240–340px。callout 容器仅占浮层本身大小，按实测宽高限制在画布内部；不得移动全画布大小的绝对定位层而撑大滚动范围。400ms transform 过渡、键盘切换和常驻内容保持。宽表格仍可局部横向滚动，曲线图不可。


## 页面身份与内容标题（2026-09-20 复核）

顶栏的 h1 是当前页面唯一的身份标题。质量报表的四个子模块正文直接从筛选区开始，不再重复页名或增加“查看……”式功能介绍。报表更新时间与北京时间放在筛选区，统计口径贴近指标保留；说明不应单独形成一层大页头。

内容标题必须表达新的业务信息：图表的“每日范围内通话”、资源详情的具体资源名称可以保留。原“工作台”统一命名为“我的待办”（侧栏、顶栏、浏览器标题），正文移除同名标题、问候说明及演示记录时间，直接显示待办范围和任务列表；列表与最近变化使用二级分区标题。禁止只为套用共享 PageHeader 而重复当前页面名称。判定依据是信息职责，不是要求所有页面都采用相同布局。


## 移除工作台（2026-09-20，替代此前待办首页方案）

按用户决定移除工作台 / 我的待办独立页面与导航入口，删除空的工作区分组。默认入口、角色切换和演示重置进入复核工单；旧 view=overview 链接替换为 view=workorders。顶栏待办通知继续承担跨模块提醒与事项直达，业务队列负责实际处理。质量报表的 module=overview（质检概览）不受影响。

## shadcn 与信息去重（2026-09-22，替代以上同类旧约定）

共同交互控件使用 components/ui 下的 shadcn 实现：Button、Input、Textarea、Select、Checkbox、Tabs、Table、Badge、Dialog、Sheet、Popover、Command、Collapsible 和 Slider。业务适配层保留权限、字段校验、中文输入法、筛选及版本逻辑。文件输入仍使用浏览器文件选择器，仅接收 UTF-8 CSV；图表 SVG、音频播放引擎和时间线语义结构保留，操作控件复用共享组件。

app/shadcn.css 是共享控件的最终样式层。遵循用户最新指令使用 shadcn neutral 主按钮；导航保留浅橙背景、移除左侧描边。业务状态保留有文字的语义色。Dialog 遮罩为 52%，替代历史 72% 参数；保留正文滚动、固定操作区与关闭后焦点恢复。通知改为顶栏 Popover，只显示事件通知；待我处理留在业务模块中。

事项详情内，来源通话和关联指标仅在标题下元数据区出现；期限仅在责任区出现。删除重复的案件事实网格、录音入口和检查范围文本。无材料时不显示空材料分区。待核对问题首屏展示命中原文。责任区合为一个连续面板，主操作优先，取消重复阶段字段。

处理记录直接展示时间线，不再套事件时间线标题卡、解释文案和重复计数；场景载入不作为业务处理记录。队列的折叠入口放在队列头，分页放在队列底；删除顶栏专注办理和重复上一条/下一条导航。

通话列表移除独立查看详情操作列，通过通话编号打开 800px 上限的右侧 Sheet。录音与证据、处理记录、关联事项为抽屉内页签；关闭保留列表筛选、分页和条目焦点。抽屉内直接阅读证据，避免叠加录音弹窗。业务处理表单仍为 Dialog，数据引擎和权限不变。

提醒坐席表单首先展示接收坐席与提醒内容；安排后续复核为可选展开项，选择复核人后填写期限。原结论确认、资源 CSV 模板/导入预览、规则草稿检查与发布不受 UI 迁移影响。


## 2026-09-22 实页回归修复

- shadcn 只提供控件交互基础；规则条目、材料条目必须显式指定纵向/横向业务布局，不能继承 Button 的默认排列。
- 表单选择器标签在上；筛选栏标签不收缩，SelectTrigger 与原生 select 使用一致的布局契约。
- 资源列表不重复页面标题；名称直达详情，次级预览入口明确命名。
- 自动候选来源仅用于原始问题；复核工单显示问题数量，逐项意见标注待主管确认。
- 暂停整改以等待申诉裁定为主状态；尚未产生的材料不提供空预览，申诉理由只在摘要完整展示一次。
- 弹窗居中使用独立 translate 属性，入场只做透明度过渡；禁止动画覆盖定位 transform 导致首帧偏移。

### 2026-09-22 资源表单收敛
- 关联规则使用 shadcn Combobox 多选输入框：名称/编号搜索、已选标签移除、列表独立滚动；浮层挂载在 Dialog 内，避免滚轮被外层锁定。Escape 只收起当前下拉框。
- SOP 编辑及导入预览每条占一行，名称与步骤同列；超长省略，悬停显示全文，保留完整数据。
- 业务范围与适用角色始终并排展示，移除适用设置的折叠层和重复标题。
- 验证：类型检查、ESLint、diff 检查通过；浏览器验证列表滚动到底、按名称和编号筛选、多选/移除、键盘选择与 Escape，以及两条长短 SOP 的单行省略。


## 2026-09-22 统一设计与丰富样例（以本节覆盖冲突旧约定）

- 统一选中态为浅橙圆角色块 `#fff3e9`、深橙文字 `#a64719`，圆角 8px。导航、页签、队列、规则列表、版本记录、指标及筛选复用；取消下划线、左侧短线和额外选中阴影。风险、成功等状态仍用业务语义色。shadcn neutral 主按钮保留，避免把普通执行按钮误当选中项。
- 队列沿用用户第二张参考的信息顺序：编号与风险、标题、坐席与业务、状态与时间。内边距 12px 14px，标题与上下文间距 6/4px；不拉高单条卡片。规则列表使用相同密度。
- 资源列表直接提供查看、编辑、删除。查看使用右侧 shadcn Sheet（最大 960px，小屏满宽），保留列表和筛选，固定关闭入口、正文独立滚动；编辑直接打开表单，CSV 导入保留。删除有确认，当前规则或规则草稿存在引用时阻止，历史版本及操作记录保留。
- 关联事项不重复 Tab 标题；名称、编号/类型、状态分层，整行可点击，只有行间分隔线，没有末行底边和圆角底线。证据材料行统一内边距，无内容时不渲染空分区。
- 跨模块进入事项，分类、分页和左侧选中项与详情同步。返回入口并入页签工具区；窄屏先呈现证据/详情，再呈现责任及操作。
- Mock 增量补充 8 通带转写证据的通话、8 条问题、4 张跨阶段复核工单、6 项业务资源及对应处理记录。资源覆盖词库/知识/SOP、发布/草稿；不覆盖已有编辑、不重置演示进度。未提供音频的样例不伪装成有录音。

- 2026-09-22 队列进一步简化：移除标题右侧收起按钮及展开入口，事项队列保持可见；不再提供额外的折叠模式。

- 关联事项可点击整行统一 8px 圆角，包含默认、悬浮及键盘聚焦状态；内边距 12px 14px，取消行底边，避免悬浮色块直角和边框残留。

- 事项详情删除组内多余横线，以标题间距、正文间距区分责任/期限/操作；保留页签与内容边界。通话表与资源表使用统一 80px 双行数据行、44px 表头，控件不额外撑高行。

- 分割线复用规范：区域边界使用 `--divider-subtle`（#edf0f3），组内靠间距分层；不在标题上下夹线，不保留末项尾线。应用到事项工作区、规则详情、资源抽屉、报表读数与分区；数据表格仍保留帮助跨列阅读的轻行线。

- 通话表、资源表和报表明细采用单条浅行线（#edf0f3）：仅从第二行起绘制顶边，取消末行底线以及筛选区与表头的重复边界。

- 全页分割线检查：通话抽屉页签只保留一层内容边界；表单分组、按钮区和通知标题不加装饰横线。通知仅条目之间保留浅线；报表指标取消竖向短线、图表读数取消顶线，表头仅保留单条浅色底线。控件描边、面板外轮廓及图表刻度线不按装饰分割线删除。

- 最新通话列表修正：每一数据行作为一个原生链接热区，支持整行点击、键盘 Enter 与修饰键打开；整行浅灰圆角悬浮/焦点反馈。通话表取消表头底线与所有数据行线，使用固定行高和间距阅读；此规则覆盖上文通话表保留行线的约定。资源表含独立编辑/删除操作，不套用整行链接。


## 复核工单2 · 独立对比版本（2026-09-22）

`?view=workorders2` 保留原复核工单入口，共用权限、筛选数据、状态与处理表单。仅此复本使用 `.review-workspace2` 作用域：桌面采用约 250px 队列、自适应证据主区、240px 责任与操作区。按最新反馈，队列、证据、责任与操作分别为三个独立白色卡片，卡片间留 16px 灰底间距，不合并中间与右侧容器；右侧责任和下一步操作用留白区分，取消内部装饰分割线。意见、原始转写片段及命中规则版本就近对照。意见明确标记为复核建议，不伪装成正式结论。

本轮经用户要求试作：确认、退回、补证常显；调整期限和转派收进“更多操作”，作为前文常显规范的本页面例外。无原音时显示缺失状态，不模拟播放。仅看逾期可切换，处理中页签明确包含待办。首次逾期以“超过办理期限”事件并入处理时间线，按实际首次逾期时间排序，展示当时截止时间，不在页签顶部或责任栏重复展示。历史数据未保存逾期时责任人时明确显示“当时责任人未记录”，不以当前负责人代替；之后延期或办结仍保留该历史事件。窄桌面将责任和动作移到详情下方；手机维持队列进入详情与返回，正文不缩小。原版视觉与工作流保持原状。

复核工单2 的队列改为分批滚动展示，替代此前的紧凑分页：顶部显示筛选后总条数，首批 10 条，接近底部 160px 时追加下一批；底部“加载更多”支持键盘与观察器不可用时的手动加载，全部展示后只显示轻量结束提示。搜索与筛选作用于完整数据集；切换筛选回到顶部，队列内切换工单保留筛选、已展示批次及滚动位置。此原型数据仍在浏览器内，当前分批的是渲染量，尚未接入服务端游标接口。其他页面保留分页。

复核工单2 的详情页签取消横贯卡片的上下分割线，使用浅橙选中态与页签下方留白分层；页签、标题和证据正文继续共用同一左对齐线。

复核工单2 三张大卡片（队列、详情、责任与操作）明确取消外描边和阴影，以白色表面、浅灰画布及 16px 列间距分区。右侧内部卡片亦无描边，责任与操作使用留白分组；输入框和按钮保留原有细描边。仅在此复本激活时，普通侧边导航文字为 #626B76，选中项沿用深橙文字和浅橙底。

复核工单2 筛选栏：搜索框、业务下拉框和导出按钮统一为桌面 40px 高、7px 圆角、1px 浅灰描边，无默认阴影；窄屏统一为 44px 高，保留键盘焦点反馈。

复核工单2 详情层级（2026-09-23）：问题与证据页签直接呈现逐问题卡片，删除“逐项核对”和“意见与证据对照”的重复标题层。每个问题独立使用浅灰表面、10px 圆角、18px 内边距（窄屏 16px），卡片间距 16px，无边框和阴影；原话不再另套底色容器，复核建议、证据入口和规则仍就近对照。无录音提示保留为轻量文字。关联事项直接显示条目，不再显示“同通话其他问题 / 处理事项”分组标题，类型由条目元信息表达。仅作用于复核工单2，原版保持不变。


## 复核工单2：问题摘要与问题详情（2026-09-23）

- 摘要卡与详情分工：卡片展示问题名称、编号、来源、风险级别、关联指标、命中规则与版本、具体触发原因、一条引用原话及复核建议。详情入口统一“查看详情”；详情包含具体条件与实际表现对照、待核实项、完整录音/上下文、人工意见、人员/时间及已有正式结论。本文前述卡片完整铺开规则和复核理由的描述由本节替代。
- 指标与规则均可点击，在同一个弹窗内查看该问题命中版本的规则配置、引用资源及例外；规则版本缺失明确展示缺失，不回退最新版本。返回问题详情恢复阅读位置。卡片时间打开对应问题详情并定位所引用的话轮；无原音时仍可定位转写，不伪装播放。
- 来源通话编号为链接式按钮，穿透到对应通话记录和完整证据。沿用原有页面返回及队列状态保存。
- 问题与证据、处理记录、关联事项共用同一内容左右边界；删去后两者额外内缩。卡片等宽，高度随内容增长。窄屏指标/规则改为单列，标题和详情入口可换行，正文不缩小。
- Finding 可保存独立的 detectionBasis（摘要、判定条件、实际表现和核对状态），不能用规则通用说明冒充本次触发原因；旧记录缺失时如实提示，保留人工意见。仅对未被操作、未形成正式结论且内容仍匹配原样例的 WO-1039 补充演示依据，并修正第二个问题的规则及引用片段；不重置用户进度。
- 本轮验证：摘要/详情/规则来回切换、来源通话穿透与返回、时间定位、三个页签边界（桌面均 848px）、390px 无横向溢出；类型检查、ESLint、构建及迁移/版本定位回归通过。


- 2026-09-23 子项边界修正：等宽验收对象是问题卡片、关联事项按钮和每条处理记录 li 本身。三类子项统一 100% 宽度、10px 圆角、18px 内边距（≤850px 为 16px）及 16px 项间距；三类子项默认均无底色、由白色父容器承托，悬浮统一使用全站 surface-subtle 浅灰色与 160ms 背景过渡；问题卡片在内部入口获得键盘焦点时同步浅灰，关联事项整行可点击并保留全站 focus 焦点轮廓，处理记录仅作阅读高亮、不添加手型或可点击语义；减少动态效果时取消过渡；时间线节点收在条目内部，不再让内容块因节点独立占列而形成另一条外边界。空态不强行套条目卡片。

- 原型界面不显示“演示 / Demo / Mock”等标识，包括顶栏、记录、表单、身份选择、辅助入口及可访问名称；用业务文案描述操作。数据层来源记录在研发文档，合成音频、缺失录音或服务未连接等影响判断的信息仍如实说明。

- 工单头部字段采用相同垂直中心线与行高；穿透文字入口不追加右箭头。复核工单2头部将问题数量替换为触发指标，按关联问题去重，使用 shadcn Badge 最多显示五个指标，多余以 +N 展开；标签 hover 使用全站底色与文字反馈，不弹预览浮层，点击直接打开命中规则弹窗；同指标对应多个问题时在该弹窗内切换对应规则及历史版本，不足五个时按实际数量展示。当前工单标题仍来源于 scope 检查范围，命名调整待确认。

- 依据 /Users/hpl/Pictures/质检/新版/对的流程图.md，问题卡片及详情不显示“待关注 / 高风险”伪状态标签；问题来源使用“规则检测 / 人工发现”普通字段，和处理状态、复核意见、正式结论区分。不得从风险程度或数据来源新增生命周期阶段。

- 复核工单2移除头部重复的“录音与证据”按钮，通话状态移至右上角。完整通话仍由来源通话编号进入；单个问题的证据、转写与规则通过查看详情或时间入口进入。通话状态与工单处理状态分别呈现。


## 指标规则与预警策略：Moss B2B 页面设计（2026-09-23）

本节替代先前将通用触发条件放在指标内的设计。

- 两个子页面使用语义页签，沿用复核工单2的分区方式：浅灰页面底色，左侧列表白卡与右侧详情白卡持续并排；选择条目只更新右侧，保留左侧筛选和滚动位置。策略筛选按风险等级，指标筛选按类别；不保留无实际区别的策略类别筛选。
- 采用本机 Moss 资产 Sora/MiSans，复制到 public/fonts/strategy；Regular 为 400，Semibold/Demibold 源文件按 500 映射。策略页样式集中在 app/strategy-design.css，选择器仅作用于该页面家族及两类编辑弹窗，不改变其他业务页。
- 详情标题 24/34px，列表标题 15px，正文/表单 14px，辅助文字 12px。主操作近黑，页签选中为中性浅灰；白色工作面、#f7f7f6 只读摘要、#e5e5e5 轻边框。主分区 12px 圆角、16px 间距；列表项 8px 圆角；编辑弹窗 16px 圆角、最大 800px。
- 指标详情按说明、引用资源、特征与策略、草稿和历史排列。各特征一行，策略条件只读显示；指标编辑只维护特有参数（如语速阈值）与策略关联。待配置和不预警区分。
- 策略详情与编辑均按检测范围、触发条件、风险与处置分组。检测角色、阶段及范围、触发方式、次数/统计窗口、持续时长回归策略；发布统一作用于关联指标。关联指标不相容的修改会明确报错。
- 桌面左列 320px（851–1250px 为 300px），右列自适应，列表和详情独立滚动。默认选中首项，选中样式与右侧详情一致。≤850px 列表/详情切换，返回保留筛选及列表位置，关联行上下重排；表单正文滚动，取消/保存固定可达。hover/focus 采用中性反馈，不用颜色单独表示状态。
- 保留草稿退出保护、错误及保存反馈、历史只读与原有权限。字段说明与真实表单归属保持一致。


### 复核工单2的分区画布

工作台主内容、队列与详情间隔、详情与操作区间隔显式使用 `--canvas`；三个主区域使用白色 surface 和 12px 圆角。分区不得仅依赖 body 底色透出，避免全局背景变化导致三栏视觉合并。内部问题、处理记录、关联事项仍保持默认透明、hover/focus 浅底的既有规则。

布局修正：策略页不再使用 `main:has(.strategy-nav)` 修改宿主背景，避免 React Activity 保留隐藏页面时影响复核工单等其他模块。


## 以复核工单2截图统一工作台（2026-09-23）

用户提供的复核工单2实页截图为这三页的最新视觉依据，替代先前策略页独立字体及筛选布局的规则。风险预警、申诉与整改采用三栏；质检策略采用两栏。共用浅灰画布、白色无描边分区、12px 圆角与16px间距；页签和筛选位于分区上方。左列250px（宽屏264px），操作列240px（宽屏256px）；标题21px、正文14px、辅助12px，沿用系统中文字体和暖橙选中态、黑色主按钮。

风险预警和申诉整改复用复核工单2的滚动队列，末尾显示加载状态，队列点击保留筛选范围。详情保留各自证据、申诉理由、整改目标、处理记录、关联事项及权限；调整期限/改派等次操作收在更多操作。风险等级、申诉/整改类型仍保留自身语义。旧版复核工单与通话记录保持原布局。

策略页搜索、类别/风险筛选、数量与新增策略移到顶部；左侧保留规则或策略列表与完整摘要，右侧保留配置与版本记录。通用条件仍仅由预警策略维护。复核工单2与新页面共享作用域样式，禁止对 main 的存在性选择器进行跨模块覆盖。851–1250px 操作栏在详情下方重排，≤850px列表和详情切换；保留可访问的返回和办理动作。


## 通话记录、业务资源与质量报表（2026-09-23）

沿用复核工单2的浅灰画布、白色无描边分区、12px圆角和16px间距，沿用当前系统字体、暖橙选中态及黑色主按钮。此节替代上述“通话记录保持原布局”的限制。

- 通话与资源保留适合横向比较的表格；页签、搜索和筛选置于灰色画布，结果表单独白色承托，分页与表格同一区域。资源类型和发布状态合并到一行筛选，页签使用共用Tabs，资源说明移入列表标题。
- 通话详情采用内容与信息/操作两区，录音证据、处理记录、关联事项仍在同一内容位置切换；较窄视口上下排列，正文滚动与关闭入口可达。业务资源详情采用灰色底、白色内容/引用/历史分区，保持版本与编辑流程。
- 质量报表四个子页共用外置筛选、独立摘要区、并排分析区与全宽明细区；图表保留自身颜色语义、数值口径、筛选、钻取和CSV。统计数值不改写，缺数据仍展示空态。
- 桌面表格内部滚动，窄屏局部横向滚动、筛选换行；不缩小全部字体、不隐藏业务字段。样式限于calls-workspace、resource-workspace、report-workspace及对应详情抽屉，不根据隐藏Activity内容修改宿主。


### 指标规则列表卡片减法（2026-09-23）

指标规则卡片以名称为第一层，标题右侧放关联指标标签，名称取自 7.9 原始指标目录（如“客户诉求与业务匹配”对应“意图识别”），不再显示“全部业务”，标题下展示“资源”和“预警”两行，不重复显示固定参数、可配参数等配置分类。标题 15px、正文/辅助 12px、关联指标标签 11px；沿用既有字体、白色工作面与浅暖选中态，无新增嵌套容器。规则编号仅保留在详情，卡片不显示编号和右箭头；底部仅在有草稿或待完善等状态时出现。

卡片不显示规则版本号；资源版本紧跟资源名称同行显示（如“声纹库 V1”），不新增版本行，多资源时分别对应各自版本。卡片不显示默认“当前生效”或重复“无需配置预警”，不逐条列出“不预警”。无需预警的指标在卡片中显示“预警　无”；可配置预警但尚未完成关联的显示“待配置”，明确全部不预警的也显示“无”。配置分类仍用于顶部筛选和详情。有效特征→策略关联以分号连接并固定为一行，超出可用宽度显示省略号；悬停通过 title 查看完整内容，点击卡片在详情查看完整关联；完整关联（包括明确不预警）和版本在详情保留。草稿、待发布、待完善关联、尚未发布仍显式标注，不把未配置当成不预警。

整张卡片保持原有选择行为，鼠标悬浮与键盘焦点通过背景和焦点轮廓反馈，不增加箭头入口。长名称与资源允许换行，预警关联仅在卡片中单行省略，不通过缩小全局字号、裁切必需内容或重置数据提高密度。预警策略卡片与资源页面不使用这套减法布局。

业务分类及指标适用业务由后台预先制定，详情中的业务范围只读，不提供客户侧业务分类配置入口。“全部业务”是后台下发的适用范围。


### 指标规则详情：检查范围与预警分离（2026-09-23）
- 详情按检查内容、业务依据、预警配置排列。关联指标和后台制定的适用业务只读展示，不重复卡片分类说明。
- 检查内容共用标签/值网格，顺序固定为关联指标、适用业务、检查目标、检查项目、判断边界、检测输出。前两项并排，长内容通栏；仅多条检查项目显示列表符号。历史缺失字段明确标注未记录，旧触发摘要独立显示为原版本说明。
- 业务依据共用引用资源、资源版本、引用范围三项字段，读取规则快照指定版本。专业性规则继续展示选中的 SOP/知识条目。无需资源的规则明确显示判断依据，资源查看/编辑仍按权限与历史状态开放。
- 保留现有白色详情区、浅灰依据条目、字号和间距体系；仅依据条目使用浅灰底，不层层套卡。移动端纵向排列，操作保留。
- 预警区域统一为检测结果、预警策略两列。详情与编辑弹窗复用检测范围、触发条件、风险等级、处置方式、接收对象、重复提醒间隔六项只读字段；自定义间隔显示秒数。操作统一为“配置预警”，弹窗标题为“配置预警 · 规则名”。明确区分待配置、不预警及不适用；不支持预警的指标显示“预警状态：不适用，该指标仅输出检测结果”。列表摘要保持原口径。
- 窄屏字段转单列，检测结果与预警策略补回行内标签，避免移除表头后失去含义；版本记录和草稿检查/发布流程保持原有契约。


### 质量报表交互统一与待办摘要移除（2026-09-23）

以现“复核工单”（原复核工单2）为基准：质量报表四个模块保留浅灰画布、12px 白色分区与16px间距；可点摘要、图例、条形图行、期间结果和跟进指标统一10px圆角，hover/focus为浅灰底，选中摘要保留暖橙底。背景变化160ms，键盘焦点使用现有focus令牌。明细表按整行呈现10px两端圆角，矩阵单元使用8px圆角，不改变热度色值。

条形进度首次出现时从左侧scaleX展开500ms，比例更新时width过渡400ms；数字直接更新。沿用趋势与环图原有动效。prefers-reduced-motion下关闭新增动画与过渡，仍呈现最终比例；定时更新不重播入场动画。

申诉与整改报表移除“当前待处理”以及复核、申诉、整改三项积压摘要，默认明细为所选期间申诉改判率。保留期间裁定/按时办结、整改状态、跟进期限及明细钻取；日期口径明确区分期间结果与当前状态快照。该调整仅删除重复待办摘要，不修改工单数据或统计计算。

窄屏沿用单列图表、筛选换行与表格局部横向滚动；行内反馈和焦点不依赖悬停才可操作。


### 问题分布卡片、图表与明细联动（2026-09-23）

问题分布的四张摘要卡仅统计支持预警的指标；选中自动候选、当前确认、人工发现或误报占比时，两张图与默认明细共同切换到对应问题集合。指标分布按主指标计数，判定构成使用同一集合的当前有效结论，点击条形或环图只细化明细、不改变顶部选中卡片；日期、业务、班组和坐席筛选保留所选卡片并重新计算。

误报占比使用自动预警中已确认误报或成立的样本。左图标题明确为“误报判定样本 · 预警指标分布”，按这批分母样本计数；右图展示成立/误报构成，中心显示误报占比。排除人工发现、证据不足、未判定和不支持预警的指标；没有分母时显示“—”，不显示0%。明细及CSV使用同一选中集合或其钻取子集。


### 坐席 / 班组卡片联动与统计范围（2026-09-23）

“仅预警指标”约束问题分布维度，不排除正常通话。通话量保留所选范围内全部已结束通话，自动处理成功占比仍为检测完成 / 全部已结束通话。坐席 / 班组四张卡片分别驱动坐席分布、指标矩阵、坐席汇总与底部明细；钻取和筛选保持当前卡片。

通话量、完成量按通话计数；确认风险通话占比仅在自动检测完成群体内按通话去重，坐席各用自己的完成数作为分母，总计用汇总分子/汇总分母，不平均坐席百分比。确认问题按问题ID计数，只纳入支持预警指标的当前成立问题。

矩阵仅展示支持预警的指标。通话模式按每格通话去重，同一通话可涉及多个指标，不能把各格相加当通话总量；确认问题模式按主指标与问题条数统计。百分比条形图使用统一0–100%尺度并标注分子/分母，无分母显示“—”。

所有报表摘要卡保持8px间距、10px圆角，选中浅橙，悬停浅灰，背景/文字过渡160ms。切换不移动卡片和页面，不做数字滚动；条形长度随数据平滑更新。已有焦点反馈与reduced-motion行为保留。


## 2026-09-23：按 TXT 落实规则配置任务（覆盖前述配置版本流程）

当前规则依次展示检查定义、业务依据、预警配置。资源动作统一为“查看内容 / 编辑内容”，通过按钮打开同一居中 Dialog 查看和编辑；指定引用条目默认过滤，可查看全部。声纹、情绪、CSV 等专属字段继续复用 ResourceFields；编辑说明共享引用范围，保存资源后返回查看态。桌面最大 880px，窄屏保留边距，正文独立滚动，关闭/保存固定可达。

配置预警按钮打开居中表单弹窗，底页维持摘要与按钮。策略摘要显示触发条件、等级与处置；查看策略按钮打开只读详情弹窗，显示检测范围、接收对象及重复提醒。配置弹窗内直接显示完整参数，不套第二层弹窗。多条策略独立判断；移除最后一条回到未选择，保留原检测结果顺序；不预警与关联策略互斥。未完成、不兼容、重复关联保存失败且保留输入。弹窗打开时底页不可操作；未保存关闭需先保存或明确放弃。打开/关闭不伸缩底页，保留筛选、当前规则、滚动位置与入口焦点。

规则、策略、资源保存一次完成真实校验与持久化，不要求额外原因，不暴露草稿检查或版本发布。配置版本号、选择、记录、对比、回退、发布管理全部列为 P2；旧存储快照仅用于兼容和保全已有检测依据。以上约定覆盖本文早期涉及配置版本和页面内展开编辑的条目，不改变复核、整改等业务处理记录。

规则详情按钮位置：业务依据的“查看内容 / 编辑内容”统一放在区块标题右侧；预警配置标题右侧仅保留“配置预警”，用于当前规则的特定参数与预警关联，不提供“前往策略管理”跳转；每条“查看策略”放在对应策略名称右侧。窄屏允许标题与操作组换行，操作组保持右对齐。弹窗底部取消/保存位置与关闭行为保持原约定。

预警配置入口使用实际保存权限判断可用性；无配置权限时保留禁用按钮与“仅质检主管可配置”提示。配置弹窗分清本规则特定参数与关联策略的只读通用参数；不将查看规则等同于获得配置权限。

## 2026-09-23：预警策略 TXT 布局

按 docs/layouts/预警策略页面-TXT布局-20260923.txt 实施：策略配置在前，关联指标规则在后。配置参数按检测范围、触发条件、风险与处置分组，采用与指标规则一致的标签/值布局；策略说明并入配置区。策略列表保留名称、范围、触发、处置和关联数，编号及生效状态只在详情呈现。配置策略与管理关联按钮在各区块标题右侧，点击打开居中弹窗；明确取消直接关闭并丢弃修改，X/Escape 保留未保存保护。新增策略先参数后可选关联。特定参数仍属指标规则，关联表只读呈现；配置版本管理保持 P2。

2026-09-23：关联指标规则列表仅在数据行之间保留分隔线，最后一行不画底部收尾线；单条数据也无底线，窄屏同样适用。

2026-09-23 二级弹窗：按 docs/layouts/预警策略与业务资源-二级弹窗-TXT布局-20260923.txt，业务资源查看从抽屉改为居中 Dialog，目录和规则入口共用 ResourceDialog；查看转编辑同层切换，保存成功关闭并刷新底页，取消直接关闭，错误保留输入。只读资源表去掉必填星号，编辑记录操作放标题右侧。策略查看和编辑共用字段分组顺序，查看详情不混入指标特定参数；选择器不重复列举全部选项。固定底部操作、正文滚动、准确恢复入口焦点与末行无收尾线。

2026-09-23 预警策略主页面与二级弹窗：以 docs/layouts/预警策略-主页面与二级弹窗-20260923.txt 为本轮布局依据。新增/编辑按名称（新增）、检测范围、触发条件、风险与处置、可选关联组织，删重复引导；固定接收对象只读展示，风险选项使用完整名称。关联选择采用平面行列表、指标/关键词筛选和仅看已选，不另叠已选摘要卡；仅保留行间分隔。保存影响和错误放固定底部，取消直接关闭，X/Escape 在同层确认。策略专用字段说明与指标大表分开；不新增版本或发布流程。
