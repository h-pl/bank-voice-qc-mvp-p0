# 6001 代码同步与验证（2026-09-24）

本记录对应分支 `codex/unified-experience-6001` 的本次提交；上次线上演示记录对应 `8e5eced72343aa0e5e7dc239d35201d67759602d`。包版本保持 0.2.4，未创建新标签。本次同步 GitHub 代码与飞书文档，未执行线上部署；代码快照与线上演示应分别核对。

## 提交范围

- 汇总 9 月 22—23 日的 6001 原型迭代：统一组件、工作区、表单、通话证据、质量报表、角色操作与样例数据。
- 对齐质检主线、主管确认、申诉回流及整改验收，补齐问题独立上下文、误报归档及通话关联；保留相应流程图、时序图、页面说明及历史记录。
- 指标规则与预警策略采用左列表、右详情；资源查看、编辑及预警配置使用居中弹窗。当前交互为填写、校验、直接保存，配置版本管理列入 P2；保留内部快照及历史业务依据。
- 策略通用字段包含角色、阶段、累计次数、统计窗口、持续时长及处置。服务中起止边界可配置；语音识别、文本纠错归类为无需预警。
- 增加模拟电话接入卡片；麦克风和上传文件状态独立，支持关闭停止拾音、音频移除及输入校验。仅为浏览器输入演示，无真实 CTI、ASR、检测或自动工单联动。
- 补齐 UI、数据、流程与验收资料、字体及导入模板；修正 ESLint 对 `.next-*` 构建缓存的忽略规则，移除测试中的未使用导入。

## 本轮验证

验证环境：Node.js 26.8.1；使用本机已安装依赖的可执行入口，与 package.json 脚本对应。

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| 静态检查 | `node node_modules/eslint/bin/eslint.js . --ignore-pattern .next` | 通过；构建目录不属于源代码 |
| 工作流回归 | `node --experimental-strip-types --test scripts/*.test.mjs` | 223 项通过，0 失败 |
| MVP 检查 | `node scripts/verify-mvp.mjs` | 通过；包含 67 项既有回归及端口、指标目录、核心资料检查 |
| 生产构建与类型检查 | `node node_modules/next/dist/bin/next build --webpack` | 通过，包含 Next.js TypeScript 检查 |
| 差异格式 | `git diff --check` | 通过 |

移除未使用导入后单独复跑对应 action-contract 测试并通过。浏览器逐页和麦克风实机验收本轮未重跑；9 月 22—23 日的实页记录保留原日期，不计作本轮新验证。真实鉴权、音频上传、检测模型、共享存储及通知服务仍待联调。

## 飞书对应

[当前 PRD](https://acnc6zeentra.feishu.cn/docx/HfCadGmEroohoOx1qikcA2yAnfh) 的资产入口、交付基线与实现差异应指向本次固定代码提交；线上旧截图继续保留 8e5eced 标签。既有业务流程与资源块保留，不以本次代码检查宣称生产验收通过。
