## 两栏布局修正（2026-09-23）

本节替代下文的卡片总览与独立详情布局结论。指标规则、预警策略均恢复左列表与右详情同时可见，浅灰画布、白色圆角分区、16px 间距与复核工单2一致。列表单列，选中项同步详情；左右独立滚动。移除 main:has(.strategy-nav) 的跨模块背景覆盖，避免隐藏的策略页影响复核工单2卡片分区。

验证：Chrome 实际查看复核工单2修复前后；1920×1080 指标切换、策略页、空搜索及重置、配置弹窗开关；390×844 列表、详情、返回和无横向溢出。TypeScript、策略组件 ESLint、git diff --check 通过。本次只调整布局与选择反馈，未修改业务配置或发布数据。

---

# 指标规则与预警策略：B2B UI 实施与验收

采用 b2b-ui-design 的 Moss 视觉基线和组件交互规范。保留 React/shadcn 技术栈，复用现有 Dialog、Select、Tabs、按钮和草稿流程。

## 本轮改动

- 指标与策略分别使用语义页签、搜索/有效筛选、概览卡和独立详情，明确返回入口。
- 指标维护特有参数和关联；策略恢复全部通用触发字段。只读摘要取实际引用策略版本。
- 同步字段说明、PRD 7.9 本地版本和 DESIGN.md；飞书未同步。
- V7 迁移只追加版本；不同旧条件分别保留为独立策略，不覆盖历史和旧草稿。

## 已验证

- 最终 112 项工作流测试通过，包括迁移、条件归属、能力适配、发布引用传播、历史、资源、权限与并发。
- Chrome 1920×1080：指标列表、策略列表、详情与关联选择；确认实际 viewport 1920×1080，页面无横向溢出，使用 Sora/MiSans 资产。
- Chrome 390×844：策略弹窗宽 366px，左右各 12px；正文滚动，底部取消/保存可达，页面宽度 390px。
- 指标关联后只读展示策略条件；不兼容的持续策略在声纹选项中禁用并说明原因。
- 修改取消触发未保存保护；通用策略累计 4 次可保存草稿，对比显示此前未启用的字段；验证草稿已放弃，未发布验收阈值。
- 搜索空态及清理筛选、历史记录保持可访问。模型、通知等实际服务不属于本轮联调。

生产构建、类型检查、涉及文件 ESLint 和 git diff --check 均通过。未提交、推送或部署。
