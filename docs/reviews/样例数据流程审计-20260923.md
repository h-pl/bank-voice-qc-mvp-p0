> 本文为早期临时纠偏记录；当前字段、场景及验证结果以 [飞书 PRD 字段与样例映射](飞书PRD-字段与样例映射-20260923.md) 为准。新样例使用 PRD revision 464，已移除证据不足作为正式结论及同通话合并普通复核的旧逻辑。

# 样例数据流程审计 · 2026-09-23

## 基线与结论

业务基线：`/Users/hpl/Pictures/质检/新版/对的流程图.md`，2026-09-22 确认版。当前项目：`/Users/hpl/Desktop/语音B端产品/质检-ui-6001`。

问题并非只在标题。原始预置数据把用于展示的多问题组合、旧版直接下发整改场景与当前流程混用，导致初始状态无法从确认流程自然产生。操作测试通过不能证明初始样例正确。

本次检查范围：完整初始化后的 32 通通话、21 个问题、12 张复核工单、1 项申诉、1 项整改及其引用关系。修复前普通复核含合并两问题的工单，整改为 2 项。规则/资源检查覆盖问题引用的历史版本、存在性和适用语义；本次不代替真实银行规则的业务审定，也不证明检测模型有效。

## 已确认并修正

| 原问题 | 修正 |
| --- | --- |
| WO-1039 人为合并两个独立预警，scope 被当成工单标题 | WO-1039 与 WO-1039-B 各对应一个问题，同通话保持关联；普通复核标题取对应问题，核实范围仍保存在工单中 |
| 同通话事件全部混入当前处理记录 | 复核工单2 仅显示本工单、所核问题和通话自身事件，其他工单单独查看 |
| 两个误报结论仍为 delivered | 改为 closed，并补齐后续优化反馈记录 |
| 两项整改的创建时间早于原通话和结论 | 当前整改先确认、分发，再接受并创建；历史标准与当前要求一致 |
| AP-1033 刚提交申诉，同时已存在 REC-1033 整改 | 保留未接受结果后的申诉，移除不应提前生成的整改；整改分支由真实后续操作生成 |
| 整改验收人和原复核质检员不一致 | 沿用原复核 Q01，保留来源路径 |
| 风险结论缺少处理要求和结论证据快照 | 补齐分发版本、期限、标准、样例要求，以及正式结论的证据、规则版本和批次 |
| “业务解释不完整”套用身份核验规则；时效/费用承诺套用意图或通知规则 | 增加对应业务条件说明规则及依据；通知策略不再充当这些问题的检测规则 |
| “重复打断”套用静默规则 | 改为静默问题，提供阈值、检测区间与后续回应；不再伪造打断现象 |
| IVR 申诉、提前挂断例外与转接问题缺少对应对话 | 补齐与各自场景一致的转写；客户陈述 IVR 完成仍只是申诉依据，未当成有效核验事实 |
| 消极表达的后续回应与确定风险建议冲突 | 改为证据不足，等待补充完整处理经过 |
| 客户否定语境标题实际引用坐席原话 | 明确为坐席否定语境 |
| 无音频的复核记录声称完成原声核对 | 改为已核对现有转写及规则依据 |
| 23 通通话时长与起止时间不一致，进行中批次却有结束时间 | 对齐时长与起止；未完成批次不写结束时间 |
| 处理日志硬编码“两项问题”，部分生成时间早于批次结束 | 按本工单的实际意见生成记录，生成事件不早于完成检测 |

## 数据与兼容性

- `lib/seed-scenarios.ts` 只构建全新初始样例，绝不对用户保存记录执行。
- `lib/prototype-seeds.ts` 统一完整初始化。旧数据仅在 revision=1、无请求、无补件、业务记录 rev=0、仅含已知预置日志且符合旧样例指纹时迁移。
- 迁移前保存 `moss-qc-mvp-p0-v2-before-seed-audit`。发生过业务操作的状态不重置；其历史样例矛盾不自动猜测修复。
- `scripts/fixtures/legacy-*.json` 仅在测试中加载，保留旧记录兼容性回归，应用不导入。
- 同通话多个风险的报表测试改为分别发布两张独立工单；不再依赖错误的合并样例。

## 验证

- 112 项测试通过（包含同期预警归档检查）：包括 8 项新增完整样例关联、时间先后、分发/接受链路、历史迁移及真实操作路径校验。
- 修改文件 ESLint 通过，Next.js 生产构建通过。
- 本地 6013 页面验证：独立标题/队列、单问题详情、同通话关联、证据不足建议及独立处理记录。

## 边界

这些是产品原型的预置业务场景。补齐的声学检测区间是样例检测结果，不代表本次实际运行声学模型；无音频的场景仍保留录音不可用提示。不能把样例检查通过表述为所有业务规则或所有用户已有数据都已验证正确。
