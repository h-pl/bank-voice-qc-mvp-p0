# 6001 实页上下文按钮复核

2026-09-23。本轮以实际页面是否能够到达并办理为标准，重新核对三张主流程、三张交接时序和八张页面流程。节点定义沿用 [逐节点核查表](流程节点与操作按钮核查-20260923.md)。

结论：主线按钮集合基本正确，但之前的动作权限测试未覆盖所有页面入口。本轮又复现两处遗漏，不能把此前“测试通过”解释为页面已经全部正确。

## 复现与修正

| 问题 | 实际页面证据 | 修正 |
| --- | --- | --- |
| 普通复核补件对执行人不可达 | 周敏的复核工单待办只有 5 个结果事项，没有 `SUP-CTX-EVIDENCE-PENDING-NEW-1`；补件可执行 `reply`，但父工单不可见。旧跳转又无条件转向父工单。 | 当前执行人无父工单权限时，在工单队列和待办通知展示独立补件；打开、通知跳转与成功提示保留可见补件目标。执行人仅见要求、提交材料和自己的操作，不扩大内部复核权限。主管继续在原工单接收。 |
| 存量退回样例丢失办理依据 | `WO-CTX-SPOT-RETURN-WO-SPOT-2` 的“处理记录”包含“请重新核对业务受理范围、查询渠道及适用时限”，但办理概览和再次提交弹窗只有旧抽检汇总。新代码保存字段并不能补回已持久化样例。 | 优先读取专用退回字段；旧记录缺字段时从同一对象的主管退回事件读取原文。办理页与重新提交表单都显示；不修改存量数据，不拼接其他工单或非主管记录。 |

补件提交/接收弹窗同时展示“补件要求”和“已提交材料”，避免单独显示处理说明而丢失操作依据。

## 本轮实页覆盖

在实际 `localhost:6001` 读取并记录了 35 个角色/对象上下文的按钮；额外打开抽检结果、关联问题、整改核验和整改材料等表单。未把代码允许执行等同于页面可达。

| 身份 | 实页对象 | 核对结果 |
| --- | --- | --- |
| 主管（17） | F-1041、F-1031、F-1037；WO-1039、WO-SPOT-2、WO-CTX-REVIEW-FP-WO-1039-B、WO-CTX-EVIDENCE-REQUEST-WO-1039-B、WO-CTX-EVIDENCE-SUBMITTED-WO-1039-B、WO-AP-AP-DECISION、WO-CTX-SPOT-FOUND-WO-SPOT-1；AP-1033、AP-AP-DECISION、AP-CTX-AP-UPHELD-AP-AP-DECISION；REC-1036、REC-UI-1、REC-UI-3、REC-CTX-REC-ADJUST-REC-1036 | 分拣三出口，确认/退回两分支，补证安排与接收，申诉工单跳转裁定，直接整改补派及调整要求。 |
| 林悦（9） | WO-UI-4、WO-AP-UI-7、WO-SPOT-1、WO-CTX-AP-RECHECK-WO-AP-UI-7、WO-CTX-SPOT-RETURN-WO-SPOT-2；AP-CTX-AP-SUBMITTED-AP-AP-SUPPLEMENT；REC-UI-2、REC-REC-RESPONSE、REC-CTX-REC-RETRY-REC-REC-FAIL | 核实/补证、申诉资料补件、接收、退回后赞同或补说明、核验通过/不通过，第二轮材料。 |
| 赵宁（4） | WO-1038、WO-CTX-REVIEW-RETURN-WO-1039、WO-CTX-LIVE-DRAFT-NEW-1、WO-CTX-SPOT-LINK-NEW-1 | 普通核实、退回核实、未结束通话只编辑草稿、仅关联同通话未定论候选。 |
| 周敏（5） | F-1035、F-CTX-AP-WITHDRAWN-F-AP-SUPPLEMENT、F-1032、AP-AP-SUPPLEMENT、REC-1036 | 知悉不代替接受；撤回后重新选择；申诉补件与撤回；直接接受后可先交整改材料。另发现普通补件缺入口。 |

其他检查在与 6001 同代码的隔离 6018 验证：陈佳的普通整改、主管意见赞同后的下一轮整改、核验失败整改、已完成整改；质检员策略/资源只读、坐席无配置菜单、主管配置预警弹窗直接保存。此前图件与全位置核查的源代码/自动化覆盖继续保留，未声称手工点过每条记录的全部身份组合。

## 完整交接验收

在 6018 真实点击：周敏从工单队列打开独立补件 → 填写材料并提交 → 待办从 6 降为 5，任务变为待主管接收 → 何晴在原工单打开接收表单，看到要求和提交材料 → 接收 → 赵宁恢复“提交核实结果 / 申请补证”。全过程不开放父工单给补件执行人。

保留已有 47 个 mock 场景；缺的是已有补件场景的页面入口，本轮无需再复制数据。6018 提交仅改变隔离验证数据，不推进用户 6001 案件。

## 自动验证与合入

- 211 项回归通过。本轮新增 4 项验证：执行人队列与通知可达、补件完整交接、另一位质检员执行补件、旧退回原因恢复且不串案。
- 类型检查通过；ESLint 无错误，既有 `action-contract.test.mjs` 保留 1 条未使用导入警告。
- 生产构建通过。逐文件哈希保护合入；保留同期通话触发规则统计与样式修改。
- 本轮不改流程图定义、不增加“更多操作”折叠、不重置现有数据；未提交远端或发布。

## 合入后 6001 回查

实际 localhost:6001 已验证：周敏待办由 5 条变为 6 条，原有 SUP-CTX-EVIDENCE-PENDING-NEW-1 可打开并显示“提交补充材料”及具体补件要求；只打开并取消，未提交。林悦打开原有 WO-CTX-SPOT-RETURN-WO-SPOT-2，办理页及提交抽检结果弹窗均显示处理记录中的主管退回原文。已恢复何晴身份。
