# 6001 迭代实施与验收

日期：2026-09-20。采纳来源：本任务新版/旧版两轮对比，以及用户“按照建议方案”指示。

## 版本与落盘

| 环境 | 内容 | 代码 |
| --- | --- | --- |
| 5001 | 本次迭代前的新版 MVP，包含现有 UI 改动 | `质检-mvp-p0` / `codex/mvp-p0-ui-refinement` |
| 5002 | MVP 前最后的完整旧版 | `语音质检-银行` / v4.11.0-private.1 / 92ae9aa |
| 6001 | 按建议采纳的对比迭代 | `质检-ui-6001` / `codex/unified-experience-6001` |

两份对比报告先写入 5001 的 docs/reviews，再随代码进入新分支：

- [需求、逻辑、架构与方案](新版与旧版-需求逻辑架构及方案汇总-20260920.md)
- [UI 交互与采纳方案](新版与旧版-UI交互对比与采纳方案-20260920.md)

6001 基于快照提交 693d6a2，完整包含 5001 当时未提交改动。使用独立工作树与 `.next`，未改变 5001 应用代码。当前依赖通过未跟踪的 node_modules 链接复用已有安装。三个端口的浏览器状态各自隔离。

## 采纳映射

| 建议 | 本次实现 | 代码边界 |
| --- | --- | --- |
| 轻量工作台 | 当前身份待办、逾期、24h 到期、授权范围内最近变化；点击回原业务对象 | Workbench；复用 notices/canSee/primaryAction |
| 菜单统一、业务布局分别设计 | 新增工作区；工单明确为复核工单；保留风险、申诉整改、通话、规则、资源、报表独立职责 | nav + Home；三角色八入口 |
| 资源分类表格 | 业务知识/词库/SOP 分类；搜索、状态、分页；名称打开预览，详情用于完整核对 | ResourceCatalog，复用 Tabs/SearchField/Pagination |
| 按操作规模选择容器 | 资源快速阅读为右侧抽屉，完整资源编辑为独立页面；规则参数仍就地编辑 | Modal.resource、InlineFormSurface、Strategy.detailOnly |
| 减少办理页滚动 | 复核意见提前，去掉复核摘要中重复的来源编号与尚未形成结论；责任与动作从顶部对齐 | 共用 Detail / CaseResponsibility / case-shell |
| 材料能看懂 | 结论依据、申诉理由、整改交接、完成材料、验收意见逐项显示摘要/版本/待补状态，逐条预览 | CaseDocuments，原始数据及绑定版本不改写 |
| 当前责任及后续去向 | 显示当前处理人、阶段、期限及分派/核查/验收后的去向 | currentOwner + nextResponsibility；动作仍由 actions 决定 |
| 资源发布与规则采用分离 | 发布只新增资源版本；明确勾选规则并填写说明后切换引用 | resource-publication.ts + apply；逐规则 rev，原子性，日志，幂等 |
| 空结果可恢复 | 资源空列表可重置，报表空时段可恢复到有数据日期段 | 共用 Empty；保留业务筛选优先恢复日期 |
| 设计语言一致 | 沿用灰白橙黑 token、共享按钮、标签、页签、筛选、弹窗和焦点行为 | DESIGN.md、UX-CONTRACT.md、workspace-system.css |

资源切换对存在参数草稿、过期规则 revision、过期资源 revision、未选择规则、无权限身份均拒绝；一次选择中任何规则冲突时整体失败。成功仅对被选规则追加版本，其余规则和已开始检测保留原快照。没有新增审批流程或真实模型检查。

## 验证

- `pnpm --config.verify-deps-before-run=false typecheck`：通过。
- `pnpm --config.verify-deps-before-run=false lint`：通过，无警告。
- `pnpm --config.verify-deps-before-run=false test:workflow`：62/62 通过。
- `pnpm --config.verify-deps-before-run=false verify:mvp`：通过；包含新增资源发布测试。
- `pnpm --config.verify-deps-before-run=false build`：通过，Next.js 生产构建。
- `npx --offline -p @google/design.md designmd lint DESIGN.md`：0 错误、0 警告；token 与运行时逐项核对。
- Premium strict audit：0 错误、0 警告、0 unresolved；证据 `premium-audit.json`。
- `git diff --check`：通过。

原始 pnpm 命令因独立工作树复用依赖链接，尝试自动重装模块并在无 TTY 时中止。通过 verify-deps-before-run=false 执行相同项目脚本，避免改变已有依赖；未绕过类型、lint、测试或构建。

### 浏览器检查

使用真实浏览器，主要为 1440×900 与 1280×720 桌面，另做 390px 基础可用性检查。功能写操作在 `127.0.0.1:6001` 独立演示存储执行，交付的 `localhost:6001` 保留正常样例。

| 场景 | 结果 |
| --- | --- |
| 分类→详情→返回 | 词库分类保持；预览 Escape 关闭、触发者焦点恢复 |
| 资源搜索无结果 | 显示空结果及重置入口；重置恢复列表 |
| 编辑后点击主导航 | 阻止离开并提示先保存或取消；原文保留 |
| 编辑后返回 | 显示继续编辑/放弃修改；必填失败聚焦说明字段 |
| 新增资源 | 保存草稿生成编号并直接打开新条目详情 |
| 发布 V2 | 资源出现 V2，三条规则仍引用 V1 |
| 单选规则切换 | 仅 R-COM-012 升级到 V2，其余两条维持 V1；刷新保持 |
| 未选择规则提交 | 显示“请选择需要切换引用的规则”，保持表单 |
| 案件材料预览 | 显示 AP-1033 绑定结论 V1、规则与检测批次；关闭回案 |
| 关联事项页签 | 列表紧接页签，没有原截图中的大段空白 |
| 工作台角色切换 | 坐席只见本人待办与允许菜单，主管数据不混入 |
| 报表空时段 | 2020-01-01～02 空结果可恢复至样例日期段 |
| 图表键盘下钻 | Enter 打开 2026-09-20 的 4 条通话，与图示一致 |
| 原生筛选器 | 展开显示现有业务选项，保留系统键盘行为 |
| 1280×720 作业页 | 无页面横向溢出；复核主动作在首屏，责任栏及正文独立滚动 |
| 窄屏资源列表 | 表格独立横向滚动，页面不横向溢出；搜索独立一行 |

## 本轮边界

此交付仍为浏览器内高保真原型。工作台使用现有待办与事件；检查为预设模拟。班组/专家、新权限体系、项目交付后台、真实模型和服务端事务属于后续阶段，没有将菜单或演示状态描述为已实现的生产能力。

1280×720 下低频动作和较长材料仍需在各自区域小幅滚动；常用办理按钮和材料入口保持就近。当前没有新增真实附件上传、远端离线同步或服务器会话机制。

## 图表常驻 callout 修订

按用户反馈，将趋势图和环形图的 callout 改为默认显示的数据标注。趋势每个点显示数值，长时间范围在图表内部滚动；环形图用引线标出非零分类、数量及一位小数占比，并对同侧标签错位。零项保留图例，无分母显示 —。悬停和键盘仅强调数据；原有点击下钻保持。

验证：typecheck、lint、62 项 workflow 测试、verify:mvp、生产构建均通过；Premium strict 审计零问题，DESIGN lint 零错误/警告。浏览器已检查默认 7 个趋势标注与 5 个检测状态标注、问题分布环图、Home/End/Enter 下钻（4 条）、环图图例下钻（19 条）、2020-01-01～07 空样本及筛选恢复。1440×1000 与默认 1280×720 桌面检查通过，浏览器无 error/warn。5001、5002 不变。

### 按 usage3 参考纠正 callout

上一节将 callout 理解为常驻数据标签，用户已明确否定；本节替代上一节的视觉方案。改为每图一个白色细边框浮层：趋势展示日期、指标值和适用的样本分母；默认选中最新有效点，整个绘图区按横向位置切换日期，移开保留。环图取消引线，用同款浮层展示选中分类、数量和占比，扇区与图例可切换，键盘焦点同样更新。

已实查用户提供的 usage3 页面与截图。浏览器确认：首次加载即有两个浮层；点击绘图区空白处（远离数据点）切换到 09/16，移到标题后仍保留；End/Enter 下钻 09/20 的 4 条明细；环图键盘切换到失败分类，数量 1、占比 4.3%；问题分布页复用相同浮层。1440×1000 视觉检查通过，控制台无 error/warn。typecheck、lint、62 项测试、verify:mvp、build、Premium strict 与 DESIGN lint 全通过。

### 图表动效与连续指针修订

实际打开 usage3 并在绘图区连续移动，DOM 确认其 tooltip 使用 transform 400ms。6001 原先直接更新 left/top，无过渡；本轮改为独立定位层的 translate3d 400ms，并按 requestAnimationFrame 合并连续指针事件。参考线与选中点采用 180ms 位移，环图描边 220ms、浮层内容 160ms，趋势入场描线与面积渐显分别 650ms/450ms。键盘选择取消待处理指针帧，减少动态效果偏好下关闭动画。

实测证据：从 09/15 连续移动到 09/19，12 次 DOM 位移采样获得 10 个不同中间位置（X 从 160.157 到 224.431 继续过渡至目标），确认发生真实渐变。环图切换时描边读数为 22.685px 与 18.315px，而非直接从 23px 跳到 18px。End/Enter 仍下钻至 09/20 的 4 条通话，控制台无 error/warn。typecheck、lint、62 项 workflow 测试、verify:mvp、生产构建、Premium strict 和 DESIGN lint 均通过。本轮未做硬件级帧率基准测试。

### B2B UI 层级与操作效率优化

按 b2b-ui-design 的 Moss platform / workbench / component 参考调整。沿用暖橙强调色和现有系统字体；本轮不声称复刻 Sora / MiSans 字体。提高主要控件至 14px、正文至 14px、详情标题至 24px，并统一队列、规则列表、资源表格、弹层的文字层级。桌面队列 / 正文 / 操作栏采用明确的间距与独立表面；当前责任只保留处理人、阶段、期限，处理后去向和辅助说明移至主要操作之后。

新增“专注办理”收起队列；上一 / 下一事项沿当前筛选结果切换并自动跨分页。收起不卸载详情，保留当前详情页签。“录音与证据”改为可读文字入口，问题结论使用独立状态标签与意见正文。补充轻量 hover、弹窗入场与资源抽屉过渡，遵循 reduced-motion；此前图表动效保持。

验收使用外部 Chrome，1920×1080 桌面基线；1280×720 检查首屏操作；390px 只做基础兼容回归。具体结果：

- 全部工单中，从第 2 项连续切换至第 6 项，分页同步为第 2 页（6–7 / 共 7），当前事项正确。
- 处理记录页签下收起 / 展开队列，页签与案件保持；边界上一 / 下一按钮禁用。
- 录音与证据预览可打开，Escape 关闭后焦点回触发按钮。
- 1280×720：复核“确认结论”位于 y=506–550，操作区域底部为约 620；申诉“受理并分派核查”同样首屏可见。低频动作在操作栏内部滚动。
- 规则可调参数进入编辑并取消；当前版本和规则保持。资源预览 Escape 关闭，搜索空结果有恢复入口，清除后恢复 3 项。
- 390px：队列打开详情后存在返回列表入口，页面宽度与视口均 390，无页面横向溢出。
- 浏览器控制台无 error / warn。测试视口已恢复。
- typecheck、lint、build 通过；workflow 62/62 和 verify:mvp 通过；git diff --check 通过。

本轮仅修改 6001 分支与本地预览，5001 / 5002 不变；不涉及真实后端、权限或业务流转规则变更。

### 操作弹窗精简

实际打开用户给定 https://moss-api-mvp-p0-compromise.vercel.app/app/api-keys 的创建弹窗（未创建密钥）。学习其单层白色工作面、字段分组、轻分隔和底部动作，未迁移 API Key 业务。

6001 的操作弹窗统一采用 action 变体：删除重复编号/操作者卡片；标题下单行表达对象；移除逐项 fieldset 套框；桌面问题标题与结论同行。证据默认仅展示已选原文，点击“调整证据”展开完整列表，收起保留选择，原文按时间排序。正文成为唯一滚动区，标题、校验反馈与底部按钮固定。点击遮罩不关闭，Escape 关闭恢复入口焦点。简单调整期限表单同样适用；资料阅读弹窗和资源抽屉不在本轮样式范围。

实页验证：1920×1080 检查结论表单层级；1280×720 中弹窗 y=24–696，操作区 y=614–696 保持首屏可见；390×844 中弹窗 x=12–378、底部操作 y=756–832，无页面横向溢出。新增证据后收起显示已选 2；空必填提交被阻止，焦点进入无需整改原因，错误提示在操作区可见，填写后清除。Escape 关闭未提交表单，入口焦点恢复；重新打开未保存输入不写入记录。调整期限简单表单正常。窄屏点击遮罩保持打开。控制台无 warn/error，测试视口已恢复。

验证：typecheck、lint、生产构建通过；workflow 62/62。未在 UI 中执行成功流转提交，业务流转由现有测试覆盖。本轮修改仅在 6001。

### 全部弹窗统一与规则参数迁移

本轮替代上一轮“仅 action-dialog”的范围限制。所有业务 Modal 统一白色表面、黑色 72% 遮罩、20px 标题、相同关闭入口与间距、固定 footer 和正文滚动；不再点击遮罩关闭。宽度依任务设置：规则参数 560px，普通操作/材料/资源 680px，长证据 960px，待办 520px。资源预览从右侧抽屉改为同款居中弹窗。移动导航保留侧栏布局但共享模态、关闭、遮罩与焦点行为。材料预览移除重复套框，以分组标题和细线区分。

规则参数改为简洁弹窗，底页当前值与尺寸保持；初始焦点进入参数，空修改原因/无变更/3–60 整数秒边界校验保留。未保存退出在同一弹窗提供继续编辑、放弃修改。保存后回本规则并显示待发布草稿，不改变生效版本。资源返回入口改为左箭头文字按钮，去掉“内容、引用与版本”，修正返回时 lastOpened 被清空导致焦点无法恢复的问题。

浏览器验收：
- localhost:6001：范围改为信用卡、空原因保存阻止并聚焦原因；Escape 提示未保存；继续编辑保留值，明确放弃后回编辑参数按钮。
- 127.0.0.1:6001 独立演示存储：阈值 2 秒被阻止；15→20 秒填写原因后保存成功，当前生效仍为 15 秒，草稿为 20 秒；刷新后弹窗回显 20。未执行发布。
- 普通案件材料弹窗约 680×469，去除原截图中大块套框与超宽布局；录音证据为 960px，长内容仅 modal-body 滚动，关闭操作固定。
- 资源预览进入详情再返回，焦点恢复到 RES-KNOW 原条目；返回箭头方向正确。
- 指标说明、待办弹窗正常开关；Escape 关闭与默认 footer 已检查。390×844 下长指标弹窗 x=12–378、y=12–832，操作区 y=756–832；参数弹窗 366×499，无页面横向溢出。移动导航正常。
- 两个浏览器标签控制台无 error/warn，临时视口恢复。
- typecheck、lint、build、workflow 62/62 通过，git diff --check 通过。DESIGN 与 UX-CONTRACT 同步更新。

本轮保持 5001/5002 与业务权限、检测、版本发布逻辑不变；测试草稿仅写入 127.0.0.1 的独立演示存储，未改变 localhost 的规则数据。


## 资源编辑弹窗漏项修复

前一轮统一了 Modal 的外观，但资源编辑和新增仍通过 embedded 使用页内表单，这是遗漏。本轮去掉这两个入口的 embedded，列表/详情保留在弹窗底层。关闭、Escape、取消都经过未保存保护，底部显示继续编辑/放弃修改；初始聚焦内容/名称，关闭恢复触发按钮焦点。资源内容输入保留 132px 最小高度。

实页验证：词库编辑以 680px 居中弹窗出现；Escape 后继续编辑保留输入，放弃后 URL 和详情不变、焦点回编辑资源。新增资源同款弹窗，长正文内部滚动，取消回新增按钮。业务知识空处理说明被阻止；在 127.0.0.1 独立演示存储填写内容及说明后保存成功，显示待发布草稿，V1 和已有引用未改变，刷新后修改草稿回显新内容。390×844 下弹窗宽 366px、底部按钮可见，临时视口已恢复。控制台无 error。localhost 未保存测试数据。

验证：typecheck、lint、build、workflow 62/62 通过；6001 已重启使用新构建。5001/5002 未修改。


## 详情返回栏一致性修复

针对资源详情文字返回与通话详情两层按钮不一致，抽取 DetailNavigation 并复用。通话取消重复灰底导航/筛选保留说明及正文外套框；返回具体来源与通话列表、上一通/下一通在一行。连续浏览通过历史步数保留原来源，列表进入不继承先前跨页来源。

验收：报表 CALL-1040 下钻 → 下一通 → 返回质量报表，仍显示原报表明细；通话列表 → CALL-1040 → 返回通话列表，URL 正确。实际通话返回栏高 57px，与资源共用同一组件及尺寸规则，截图检查完成。lint、build（含 TypeScript）通过。6001 已加载最终构建；未修改 5001/5002。


## 工作台与分析页结构一致性

已将两页纳入白色主面板体系，工作台、分析、资源共享 PageHeader；工作台及分析共享 MetricSummary。去掉工作台大标语和重复筛选页签；主区处理待办、辅区最近变化，桌面待办内部滚动。分析的页签、筛选、统计、图表与明细用同一面板和细线组织，保留业务布局与原图表动效。

实页验收：工作台逾期为 0 时显示空态，返回全部恢复 7 条，24 小时内为 2 条；打开 WO-1039 后返回工作台仍保持到期筛选。桌面 958px 高度下主面板 842px，待办正文可视 491px/内容 648px，文档总高仍为 958px；390px 视口无横向溢出、7 条待办均存在，临时视口已恢复。分析四个页签均正常；信用卡筛选显示 8 通，点击当日图表明细显示 2 通，数据联动正常。浏览器无 error。typecheck、lint、build（含最终 TypeScript）通过，git diff --check 通过。6001 使用最终构建；5001/5002 未修改。


## 质量报表四页签及趋势图双向滚动修复

实测原画布 907×340，内容滚动范围却为 1567×372：全画布大小的 qa-callout-anchor 被 transform 移动后撑大容器；qa-chart-scroll 的 overflow-x:auto 同时使纵轴为 auto。移除图表滚动包装和 520px 最小宽度，callout 按自身实际尺寸定位，ResizeObserver 同步 SVG 坐标、命中点与可用宽度。模块标题改为质量报表，四个页签及各自筛选/内容归属明确。

浏览器验证：桌面画布 client/scroll 均 907×340，1280px 窗口为 620×276，390px 窗口为 340×240，后者 callout 完整位于画布内，文档宽度为 390px。比例指标仍显示样本，Home/End 切换正常，查看当日明细显示 5 通，四个页签均正常切换；最终图内 auto/scroll 容器为 0。callout 保留 0.4s 过渡，浏览器无 error。typecheck、lint、build 通过，临时视口已恢复，6001 已加载新构建。未修改 5001/5002。


## 质量报表导航层级纠正（最终）

用户明确要求的是左侧“质量报表”分组与四个平铺子模块。前一轮把子模块理解成页面页签错误，临时折叠父菜单也多了一层；最终移除原“分析”标签和可折叠父菜单，以“质量报表”分组直接承载质检概览、问题分布、坐席 / 班组、申诉与整改，菜单图标、对齐及选中状态和其他业务模块一致。

新增 report-navigation 配置；各模块使用独立 URL 参数 module=overview/issues/teams/improvement；页面内无四页签。各子模块独立保留筛选、分页和明细选择。浏览器已验证问题分布→坐席/班组→后退保留信用卡筛选，刷新 issues 仍显示问题分布；第四子模块可打开；通话下钻返回质检概览及原地址，控制台无 error。最终导航树显示质量报表分组直接带四个链接，父级按钮和页面页签数量均为 0。lint、build（含 TypeScript）通过。
